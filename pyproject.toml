[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-afterlife"
version = "0.1.0"
description = "Afterlife — ghost tile persistence, reef storage, resurrection protocol"
license = {text = "MIT"}
requires-python = ">=3.8"
